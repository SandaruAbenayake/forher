import { useState } from 'react'; import { useNavigate } from 'react-router-dom'; import { Shell, Button } from '../components/Layout'; import { useDate } from '../context/DateContext';
export default function FirstPage(){const go=useNavigate(),{visitorName,update}=useDate();const [name,setName]=useState(visitorName||'');const ready=name.trim().length>0;return <Shell step={1}><p className="eyebrow">Before we start</p><h1>Add your name, sweety 💕</h1><p>So I know exactly who I'm sending this to.</p><input className="date" value={name} onChange={e=>setName(e.target.value)} placeholder="Your name" maxLength={40} /><div className="actions"><Button disabled={!ready} onClick={()=>{update({visitorName:name.trim()});go('/ask')}}>Continue →</Button></div></Shell>}
