import { useState } from 'react'; import { useNavigate } from 'react-router-dom'; import { Shell, Button } from '../components/Layout'; import { useDate } from '../context/DateContext'; import { sendConfirmationEmail } from '../lib/emailjs';
export default function ConfirmPage(){const go=useNavigate(),{selectedDate,selectedTime,selectedFoods,update}=useDate();const [sending,setSending]=useState(false);const date=selectedDate?selectedDate.toLocaleDateString(undefined,{weekday:'long',month:'long',day:'numeric'}):'Not selected';const confirm=async()=>{setSending(true);try{await sendConfirmationEmail({date,time:selectedTime,foods:selectedFoods})}catch(e){console.warn('Email could not be sent',e)}update({isConfirmed:true,currentStep:6});go('/celebrate')};return <Shell step={5}><p className="eyebrow">One last check</p><h1>Best Friend Date Confirmed!</h1><div className="summary"><p><b>Date:</b> {date}</p><p><b>Time:</b> {selectedTime || 'Not selected'}</p><p><b>Food:</b> {selectedFoods.join(', ') || 'Not selected'}</p><hr/><em>“I can't wait to spend time with you!”</em></div><div className="actions"><Button disabled={sending} onClick={confirm}>{sending?'Sending…':'Confirm 💌'}</Button><Button className="secondary" onClick={()=>go('/schedule')}>Edit</Button></div></Shell>}
