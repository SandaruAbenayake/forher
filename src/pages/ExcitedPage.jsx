import { useEffect } from 'react'; import confetti from 'canvas-confetti'; import { useNavigate } from 'react-router-dom'; import { Shell, Button } from '../components/Layout'; import { useDate } from '../context/DateContext';
export default function ExcitedPage(){const go=useNavigate(),{update}=useDate();useEffect(()=>{confetti({particleCount:100,spread:75,origin:{y:.65}})},[]);return <Shell step={2}><div className="cat">😻</div><h1>WAIT... YOU ACTUALLY SAID YES??</h1><p>I was preparing for rejection.</p><Button onClick={()=>{update({currentStep:3});go('/schedule')}}>Continue →</Button></Shell>}
