import { useNavigate } from 'react-router-dom'; import { Shell, Button } from '../components/Layout'; import EscapingButton from '../components/EscapingButton'; import { useDate } from '../context/DateContext';
export default function AskPage() { const go=useNavigate(), {update}=useDate(); return <Shell step={1}><p className="eyebrow">A tiny important question</p><h1>Will you be my good friend?</h1><p>This is a yes or yes situation btw.</p><div className="actions"><Button onClick={()=>{update({hasAnsweredYes:true,currentStep:2});go('/excited')}}>YES 💖</Button><EscapingButton/></div></Shell>; }
